RSpec.describe Aws::Cloudformation::Helper do
  it "has a version number" do
    expect(Aws::Cloudformation::Helper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
